"""SDK identity constants reported via telemetry.sdk.* and the User-Agent."""

from __future__ import annotations

VERSION = "0.1.0"
"""The SDK version reported via telemetry.sdk.version and the User-Agent."""

SDK_NAME = "groundcover-python"
"""The telemetry.sdk.name value identifying this SDK."""

SDK_LANGUAGE = "python"
"""The telemetry.sdk.language value."""
